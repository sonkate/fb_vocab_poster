"""Draft → review → narrate → render → post English vocabulary lessons.

Layered so dependencies point inwards: `domain` <- `application` <-
`infrastructure`/`interface`, wired together in `container.py`.
"""
