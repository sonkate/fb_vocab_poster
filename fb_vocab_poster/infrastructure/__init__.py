"""Adapters: everything that talks to a library, a network or the disk.

Each module here implements a port declared in `application.ports`. Nothing in
`domain` or `application` imports from this package.
"""
