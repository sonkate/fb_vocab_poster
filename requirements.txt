anthropic>=0.40.0
gTTS>=2.5.1
moviepy>=1.0.3
Pillow>=10.0.0
requests>=2.31.0
python-dotenv>=1.0.0
# Only Firestore is used, so not the whole firebase-admin SDK
google-cloud-firestore==2.21.0

# dev
pytest>=8.0.0
